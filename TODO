Chores:

- Fix numerous memory leaks associated with ipc_run() and return, and
  others.
- Fix dynamic building and linking of plugins
- Add HTTP authentication
- Fix http binding on other IPs.
- Far far far more error handling
- Fix vadmin with regards to n-arg and shmlog re-parsing during reconnect
- Fix VSS_open error messages (e.g: use logger()).
- Fix frontend glitching when varnishd is down.
- Go over how various plugins react to varnish being down.


In addition there are a number of big items on the todo that are not
covered here (e.g: new plugins).
